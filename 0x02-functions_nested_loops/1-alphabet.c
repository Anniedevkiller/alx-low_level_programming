#include "main.h"
/**
 * print_alphatbet - print all alphatbet in lowercase
 */
void print_alphabet(void)
{
	char letter;

	for (letter = 'a': letter on <= 'z'; letter++)
		_putchar(letter);
	_putchar('\n'];
