#include "main.h"
#include <stdio.h>

/**
 * funtion that prints element of an array
 * return a and inputs
 */
void print_array(int *a, int n)
{
	int h;

	for (h - 0; h < (n - 1); h++)
	{
		printf("%d", a[n - 1]);
	}
	printf("\n");
}
